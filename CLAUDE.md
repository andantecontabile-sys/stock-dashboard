# 每日選股儀表板（Daily Equity Dashboard）

本機一鍵執行，產出單一 `dashboard.html`。不架伺服器、不上雲、不常駐。

---

## 0. 鐵則（違反即為 Bug）

1. **禁止自動化存取 Facebook。** 不寫爬蟲、不做無頭瀏覽器登入、不繞驗證碼。粉專內容一律由 `inbox/` 匯入。
2. **禁止繞過任何驗證碼。** 遇到 CAPTCHA 的資料源，改走人工匯入或付費 API。
3. **所有 API 欄位名以實際打一次回應為準。** 不得沿用二手教學文的欄位名，證交所改過多次。首次接每個端點時，先 dump 原始 JSON 存到 `debug/` 再寫解析。
4. **台股一律使用還原股價**（除權息還原）計算歷史分位。未還原的價格只能用於顯示「今日收盤」。
5. **估值模型輸出區間，不輸出單一目標價。** 五個模型不取平均。
6. **籌碼面輸出「觀察等級」，不輸出「買進訊號」。** 文案不得出現「建議買進」「進場點」等字樣，改用「條件符合數 / 觀察等級」。
7. 任何無法取得的資料，在 HTML 上明確標示 `資料缺漏`，不得以 0 或前值靜默填補。

---

## 1. 執行方式

```
./run.command        # macOS
run.bat              # Windows
```

流程：讀 `inbox/` → 抽標的 → 抓數據 → 算估值 → 算籌碼 → 產出 `output/dashboard_YYYYMMDD.html` → 自動開啟瀏覽器。

全程無需人工介入。單次執行預期 3–8 分鐘（視標的數量）。

---

## 2. 資料收集層

### 2.1 粉專與週報（人工匯入）

老爺在日常瀏覽時，把有價值的貼文丟進 `inbox/`：

- `inbox/*.txt` / `*.md` — 直接貼上的貼文全文
- `inbox/links.txt` — 一行一個 URL（可公開存取的網頁才會被抓）
- `inbox/*.png` / `*.jpg` — 手機截圖，由 OCR 或視覺模型讀取

程式啟動時掃描 `inbox/`，處理完移到 `inbox/archive/YYYYMMDD/`。

**格式約定**：每則貼文開頭可選填一行 metadata，缺省時由模型推斷。

```
::source=粉專名稱 ::date=2026-08-10 ::stance=看多
（以下為貼文內容）
```

### 2.2 電子報（自動）

若有 email 訂閱來源，走 IMAP 讀取專用信箱。設定放 `.env`：

```
IMAP_HOST=
IMAP_USER=
IMAP_PASS=            # 使用應用程式專用密碼，不得使用主密碼
IMAP_FOLDER=Newsletters
IMAP_LOOKBACK_DAYS=8
```

---

## 3. 標的抽取層

用 LLM 做命名實體辨識，**不要用 regex**。中文個股簡稱（華新科／華新、聯電／聯發科、台達電／台達）用字串比對必定出錯。

輸出 schema：

```json
{
  "ticker": "2492",
  "name": "華新科",
  "market": "TWSE",          // TWSE | TPEX | US
  "source": "某粉專 2026-08-09",
  "stance": "bullish",        // bullish | bearish | neutral | unclear
  "claim": "原文主張的一句話摘要",
  "confidence": 0.9
}
```

驗證步驟：抽出的代號必須能在 `data/ticker_master.csv` 中找到，找不到就丟進 `output/unresolved.json` 並在 HTML 上列出，**不得靜默丟棄**。

`ticker_master.csv` 每週重建一次，來源為 TWSE / TPEx 的上市上櫃公司基本資料端點。

---

## 4. 數據層

### 4.1 台股

| 用途 | 來源 |
|---|---|
| 日成交、收盤、成交金額 | `https://openapi.twse.com.tw/`（上市）、`https://www.tpex.org.tw/openapi/`（上櫃） |
| 三大法人買賣超（個股） | 同上，兩市場各自的 T86 類端點 |
| 融資融券、借券 | 同上 |
| 本益比／殖利率／股價淨值比（官方版） | TWSE BWIBBU 類端點 |
| 財報原始數據 | 公開資訊觀測站 |
| 期貨籌碼（選配） | `https://openapi.taifex.com.tw/` |

### 4.2 美股

| 用途 | 來源 |
|---|---|
| 財報原始數據 | **SEC EDGAR XBRL `companyfacts` API（一手，優先）** |
| 股價 OHLCV | yfinance（免費，非官方，需容錯）或付費源 |
| 無風險利率 | FRED（DGS10） |

**優先序原則：官方 > 開源封裝 > 付費商業源 > 網頁解析。** 永遠不從財經媒體網頁刮取數字。

### 4.3 快取

所有 API 回應寫入 `cache/{source}/{date}/{key}.json`，同日重跑直接讀快取。財報資料快取 30 天。

---

## 5. 估值層（五模型 + 一對照）

每個模型獨立輸出 `{low, mid, high, applicable, warnings[]}`。`applicable=false` 時仍要顯示，並說明為何不適用。

### M1 · P/E 河流圖
近 5 年每日 `P/E = 還原收盤價 / TTM EPS`，取 10/25/50/75/90 分位。輸出現價所處分位。
**警示條件**：若標的屬景氣循環股（被動元件、面板、記憶體、航運、鋼鐵、塑化），自動加註「循環股於獲利高峰時 P/E 最低，本模型可能給出反向訊號」。

### M2 · P/B 河流圖
`P/B = 還原收盤價 / 每股淨值`，淨值按季更新並向前填補至下一季公布日。分位同 M1。
**警示條件**：當期有資產減損或一次性沖銷時加註。

### M3 · PEG
`PEG = P/E ÷ (未來 3 年 EPS CAGR × 100)`。成長率來源優先序：法人一致預估 → 過去 3 年實績 CAGR。
必須同時輸出所採用的成長率數值與來源，讓使用者自行判斷。

### M4 · DDM／殖利率法
- 高登模型：`V = D₁ / (r − g)`，僅在 `r > g` 且近 5 年配息不中斷時啟用。
- 殖利率法：近 5 年殖利率分位反推價格 `P = 每股現金股利 ÷ 殖利率分位`。

**警示條件**：偵測到當年度股利中含一次性業外收益貢獻時加註。

### M5 · 兩階段 DCF（FCFF）
```
FCFF = EBIT × (1 − 稅率) + 折舊攤銷 − 資本支出 − 淨營運資金變動
第一階段：5 年，成長率取近 3 年營收 CAGR 與法人預估較低者
第二階段：永續成長率 g，預設 2.0%（台股）／2.5%（美股）
折現率：WACC
企業價值 EV → 減淨負債 → 權益價值 → 除以流通在外股數
```
**必須輸出敏感度矩陣**：WACC ±1.0pp × g ±0.5pp 的 3×3 表格。單點 DCF 值無意義。

### X · Fed Model 對照（非估值，橫向參考）
`盈餘殖利率 E/P` vs 對應市場 10 年期公債殖利率。台股對照台灣 10Y，美股對照 US 10Y。輸出利差。

### 綜合輸出（重點）
不取平均。輸出三項：
1. 五個模型各自的估值區間帶狀圖 + 現價位置
2. **模型分歧度**：五個 mid 值的變異係數（CV）。CV 高代表市場對此標的的定價邏輯正在切換 —— 這本身是重要資訊，需醒目標示。
3. 現價落在幾個模型的「低估區」內（0–5 分）

---

## 6. 籌碼層（四項）

### F1 · 三大法人買賣超 ✅ 官方免費
輸出：前一交易日買賣超張數、**5 日累計、20 日累計**、佔當日成交量比重。
單日數值僅供顯示，**評分只採用 5 日與 20 日累計**。

### F2 · 成交金額與量能 ✅ 官方免費
`量能比 = 當日成交量 / 20 日均量`。搭配價格漲跌判斷價量配合或背離。

### F3 · 資金流向 MFI / OBV ✅ 自行計算，零成本
```
典型價 TP = (H + L + C) / 3
資金流 RMF = TP × Volume
14 期：MFI = 100 − 100 / (1 + 正向 RMF 總和 / 負向 RMF 總和)
OBV：收盤 > 前收 → +Volume；< 前收 → −Volume；= → 0，累加
```
MFI > 80 超買、< 20 超賣（僅為傳統閾值，非本專案的判斷依據）。
真正要看的是 **OBV 與價格的背離**：價創新高而 OBV 未創新高 → 標示「量價背離」。

### F4 · 券商分點主力買賣超 ⚠️ 受限
官方來源為 TWSE `bsr.twse.com.tw`（買賣日報表查詢系統）與 TPEx 的券商買賣證券日報表查詢系統。兩者皆為**單股查詢 + 圖形驗證碼**，且據了解僅提供最近一個交易日，不保留歷史。

因此本專案的處理方式：
- **不寫自動化抓取**（見鐵則 2）。
- 提供 `inbox/bsr/` 資料夾，老爺若手動查詢並下載 CSV，程式自動併入。
- 無資料時，F4 權重歸零並在 HTML 明確標示「分點資料未提供」，其餘三項權重按比例放大。
- 若日後採購商業資料源（CMoney／XQ／富果等），在此處新增 adapter。

### 觀察等級（非買進訊號）
條件式計分，每項符合得 1 分，總分 0–7：

| # | 條件 |
|---|---|
| 1 | 5 日法人累計買超 > 0 |
| 2 | 20 日法人累計買超 > 0 |
| 3 | 法人買超佔 5 日成交量比重 > 3% |
| 4 | 量能比介於 1.0–2.5（過熱排除） |
| 5 | OBV 20 日斜率 > 0 |
| 6 | 無量價背離 |
| 7 | 分點資料顯示買超集中度上升（無資料則本項不計） |

輸出文案格式：`觀察等級 5/7（分點資料未提供）`。
**HTML 上必須固定顯示一行**：籌碼指標為事後統計，不具預測性，且在除權息、指數調整、ETF 換股、借券還券日易產生假訊號。

---

## 7. 輸出層

單一自包含 HTML：`output/dashboard_YYYYMMDD.html`
- Chart.js 走 CDN，離線時退化為表格
- 版面：摘要卡片 → 台股區 → 美股區 → 未解析標的 → 資料缺漏清單
- 每檔標的一張卡：來源與原文主張 / 五模型帶狀圖 / 分歧度 / 觀察等級 / 資料時間戳
- **每個數字旁標註資料日期**，跨市場時區容易誤讀

---

## 8. 台股專屬地雷

- 還原股價：未還原則 P/E 與殖利率的歷史分位全錯，高息股尤甚
- 財報公布期限（一般公司）：Q1 5/15、Q2 8/14、Q3 11/14、年報 3/31；金融業另有規定 —— **實作前請至公開資訊觀測站確認當年度規定**
- 處置股：撮合方式改變會扭曲量能比，需偵測並標示
- 漲跌幅 ±10% 限制：連續漲跌停期間的 MFI／OBV 失真
- 上市與上櫃的端點與欄位不同，不可共用解析器
- 存託憑證（-DR）、KY 股、特別股需特殊處理

---

## 9. 開發節奏

| Sprint | 範圍 | 完成標準 |
|---|---|---|
| 1 | 台股 + M1/M2 + 手動 inbox | 對單一標的（建議 2330）跑通全流程，數字與公開資訊觀測站對得上 |
| 2 | 美股（SEC EDGAR XBRL 欄位對應）+ M3/M4/M5 | 五模型齊備，DCF 敏感度矩陣正確 |
| 3 | 籌碼層 F1–F4 + HTML 美化 | 觀察等級可用，資料缺漏處理正確 |

**Sprint 1 未通過驗證前，不得進入 Sprint 2。**

---

## 10. 驗證要求

- 每個估值模型都要有單元測試，用手算過的固定輸入驗證輸出
- 每次 commit 前執行 `pytest`
- 新接一個 API 端點時，先寫一支只印出原始回應的腳本，人工確認欄位後再寫解析
- 任何數字若無法追溯到官方來源，標示為 `未驗證` 並在 HTML 上以不同顏色呈現
